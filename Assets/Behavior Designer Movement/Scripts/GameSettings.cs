using UnityEngine;

public class GameSettings
{
    //These hold pre determind numbers while the scenes load.
    public static int mapWidth = 40;
    public static int mapHeight = 20;

    public static bool isDataLoaded = false; //A flag so the game knows if we actually picked a size.
}
